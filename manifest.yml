---
applications:
  - name: cloud-alm-mcp
    memory: 256M
    disk_quota: 512M
    instances: 1
    buildpacks:
      - nodejs_buildpack
    command: npm start
    env:
      NODE_ENV: production
      RUNTIME_MODE: destination
      MCP_TRANSPORT: http
      EXTERNAL_CALLS_ENABLED: false
